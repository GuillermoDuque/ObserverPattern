class ObservableData:
    """
    Simple class with a constructor that will receive the values
    that we want to exchange between the objects
    """

    def __init__(self, name, data):
        self.name = name
        self.data = name
